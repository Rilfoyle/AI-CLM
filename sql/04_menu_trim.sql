SET NAMES utf8mb4;

-- =====================================================================
-- CLM POC 菜单裁剪：隐藏合同系统不需要的模块入口（仅 visible=0，不删数据、不影响权限）
-- 保留：系统管理(1)、基础设施(2)、合同管理(7000)、工作流程(1185)
-- 可重复执行；恢复方法见文件末尾
-- =====================================================================
UPDATE `system_menu` SET `visible` = b'0', `updater` = '1', `update_time` = NOW()
WHERE `id` IN (
  1254,  -- 作者动态（外链）
  2159,  -- Boot 开发文档（外链）
  2160,  -- Cloud 开发文档（外链）
  1117,  -- 支付管理
  1281,  -- 报表管理
  2262,  -- 会员中心
  2362,  -- 商城系统
  2084,  -- 公众号管理
  2397,  -- CRM 系统
  2563,  -- ERP 系统
  6400,  -- WMS 系统
  5100,  -- MES 系统
  2758,  -- AI 大模型
  4000,  -- IoT 物联网
  6500,  -- IM 即时通讯
  5      -- 工作流程下的「OA 示例」（请假 demo，与合同无关）
);

-- 恢复某个入口（示例：报表管理）：
-- UPDATE `system_menu` SET `visible` = b'1' WHERE `id` = 1281;
